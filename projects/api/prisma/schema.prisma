generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Country {
  id        String   @id @default(uuid()) @db.Uuid()
  name      String   @unique
  tax       Float
  createdAt DateTime @default(now()) @map(name: "created_at") @db.Timestamptz()
  updatedAt DateTime @default(now()) @updatedAt @map(name: "updated_at") @db.Timestamptz()

  @@map(name: "countries")
}

<<<<<<< HEAD
model Brand {
  id        String   @id @default(uuid()) @db.Uuid()
  name      String   @unique
  logo      String
  createdAt DateTime @default(now()) @map(name: "created_at") @db.Timestamptz()
  updatedAt DateTime @default(now()) @updatedAt @map(name: "updated_at") @db.Timestamptz()

  @@map(name: "brands")
=======
model Category {
  id        String   @id @default(uuid()) @db.Uuid()
  name      String   @unique
  image     String
  createdAt DateTime @default(now()) @map(name: "created_at") @db.Timestamptz()
  updatedAt DateTime @default(now()) @updatedAt @map(name: "update_at") @db.Timestamptz()

  @@map(name: "categories")
>>>>>>> 72db872 (feat: add categories backend)
}

model Brand {
  id        String   @id @default(uuid()) @db.Uuid()
  name      String
  logo      String
  createdAt DateTime @default(now()) @map(name: "created_at") @db.Timestamptz()
  updatedAt DateTime @default(now()) @updatedAt @map(name: "updated_at") @db.Timestamptz()

  @@map(name: "brands")
}
